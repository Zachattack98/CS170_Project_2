#ifndef PROBLEM_H_
#define PROBLEM_H_

#include <bits/stdc++.h>
#include "Node.h"
#include "Tree.h"

using namespace std;

class Problem {
  public:
    //int expandN;    //expanding an individual node
    //int maxqueuesize = 1; //max queue size is always set 1 at start, regardless of the puzzle
    int algorithmchoice;
    int goal_state; //goal state contains all features used in tree

};

#endif
